%{mode, max}.
{mode, {rate, 1}}.

{duration, 10}.

{concurrent, 1}.

{driver, basho_bench_driver_udon}.

{operations, [{update_handoff, 1}]}.

{key_generator, {uniform_int, 20}}.

{value_generator, {fixed_bin, 20}}.

{udon_nodes, ['udon@zy-computer.local']}.
{udon_nodes_cookie, 'udon'}.
